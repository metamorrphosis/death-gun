on:
  push:
  pull_request:
jobs:
  send-request:
    runs-on: ubuntu-latest
    steps:
      - env:
          DAKI_API_TOKEN: ${{ secrets.DAKI_API_TOKEN }}
        run: |
          curl --request POST \
          --url https://pterodactyl.file.properties/api/client/servers/f29d65b1/power \
          --header "Authorization:ptlc_RuxyUhGMjyHRm8j1uwvOQZpC6SQruU8EqV5fqLdGk7z" \
          --header "accept:application/json" \
          --header "content-type:application/json" \
          --data '{"signal":"restart"}'
